{
  "name": "comic-creator-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build:packages && npm run build:backend",
    "build:packages": "cd packages/utils && npm run build && cd ../common-types && npm run build",
    "build:backend": "cd apps/backend && mv package.json.bak package.json && npm install && npm run build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "start": "cd apps/backend && node dist/index.js"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "npm@10.8.3"
}